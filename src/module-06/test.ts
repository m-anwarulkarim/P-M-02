// 🌟 Enum in TypeScript

/* 
============================================
🔹 Enum কী?
============================================

Enum (পূর্ণরূপ: Enumeration)
মানে হলো — “নির্দিষ্ট মানগুলোর তালিকা”।

👉 সহজভাবে: Enum ব্যবহার করা হয় তখন, 
যখন কোনো ভ্যালু নির্দিষ্ট কিছু মানের মধ্যেই সীমাবদ্ধ থাকবে।

উদাহরণ:
ধরো, ইউজারের Role শুধু ৩ ধরনের হতে পারে —
- Admin
- Editor
- Viewer
তাহলে Enum হলো এই নির্দিষ্ট তিনটি মানের একটি নির্ধারিত তালিকা।
*/
/* 
============================================
🔹 ধরো তুমি একটা user system বানাচ্ছো।
প্রতিটা ইউজারের একটা নির্দিষ্ট role থাকবে:
"Admin" | "Editor" | "Viewer"
============================================
*/

// ============================================
// 🧩 ১️⃣ Enum ছাড়া (type দিয়ে)
// ============================================

// আমরা union type ব্যবহার করতে পারি নির্দিষ্ট মানগুলোর জন্য

type UserRoleType = "Admin" | "Editor" | "Viewer";

const checkAccessByType = (role: UserRoleType) => {
  if (role === "Admin") return "🔑 Full Access Granted!";
  if (role === "Editor") return "🛠️ Limited Access Granted!";
  return "👀 View Only Access!";
};

console.log(checkAccessByType("Admin")); // ✅ কাজ করবে
// console.log(checkAccessByType("admni")); ❌ Error: Type '"admni"' is not assignable to parameter of type 'UserRoleType'

/* 
✅ সুবিধা:
- কোড সহজ, Enum declare করতে হয় না।
- ছোট প্রজেক্টে বা ছোট list হলে ভালো সমাধান।

❌ অসুবিধা:
- বড় প্রজেক্টে একই টাইপ অনেক জায়গায় ব্যবহার করলে duplication হয়।
- যদি role নাম পরিবর্তন করতে হয়, একাধিক জায়গায় update করতে হয়।
- Autocomplete সাজেস্ট করে না (enum-এর মতো সুন্দরভাবে না)।
*/

// ============================================
// 🧩 ২️⃣ Enum ব্যবহার করে (আরও টাইপ-সেফ)
// ============================================

enum UserRoleEnum {
  Admin = "Admin",
  Editor = "Editor",
  Viewer = "Viewer",
}

const checkAccessByEnum = (role: UserRoleEnum) => {
  if (role === UserRoleEnum.Admin) return "🔑 Full Access Granted!";
  if (role === UserRoleEnum.Editor) return "🛠️ Limited Access Granted!";
  return "👀 View Only Access!";
};

console.log(checkAccessByEnum(UserRoleEnum.Admin)); // ✅ কাজ করবে
// console.log(checkAccessByEnum("Admin")); ❌ Error: Argument of type '"Admin"' is not assignable to parameter of type 'UserRoleEnum'

/*
✅ Enum ব্যবহারের সুবিধা:
----------------------------------
1️⃣ Type-safety: ভুল মান দিলে TypeScript তৎক্ষণাৎ Error দেখাবে।
2️⃣ Centralized Control: Enum এক জায়গায় define করলে, পরে যেকোনো জায়গায় import করে ব্যবহার করা যায়।
3️⃣ Auto-complete: IDE তে Enum মানগুলো সাজেস্ট করে (Developer-friendly)।
4️⃣ Maintainable: বড় প্রজেক্টে manage করা সহজ।

❌ Enum এর ছোট অসুবিধা:
- সামান্য বেশি কোড লাগে (একটা Enum declare করতে হয়)।
- Type union এর তুলনায় একটু ভারী abstraction।
*/

// ============================================
// 💬 সংক্ষেপে:
// ============================================
//
// 🔸 Type দিয়ে: সহজ, ছোট প্রজেক্টে পারফেক্ট।
// 🔸 Enum দিয়ে: নিরাপদ ও maintainable, বড় প্রজেক্টে সেরা।
