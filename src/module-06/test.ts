/* 

## 🔹 ধরো তুমি শুরুতে লিখলে:

```ts
const Role = {
  ADMIN: "ADMIN",
  USER: "USER",
  GUEST: "GUEST",
} as const;
```

### 👉 এখানে কী ঘটছে?

* `Role` হলো একটা **object**, যার তিনটা key আছে:
  `ADMIN`, `USER`, `GUEST`
* আর প্রতিটার value হলো একই নামের string।
* `as const` বলছে →
  “এই object-এর প্রতিটা property immutable (readonly) হবে,
  এবং এর value গুলোকে general string না ধরে literal হিসেবে ধরবে।”

মানে 👇
`"ADMIN"` আর `"USER"` এখন সাধারণ string না, বরং **specific literal type**।

---

## 🔹 এখন চল দেখি TypeScript ভিতরে কী বুঝছে

```ts
typeof Role
```

এইটা দিলে TypeScript `Role` object-এর structure চিনবে, যেভাবে লিখেছো ঠিক সেভাবে 👇

```ts
{
  readonly ADMIN: "ADMIN";
  readonly USER: "USER";
  readonly GUEST: "GUEST";
}
```

---

## 🔹 এখন তুমি যদি শুধু key গুলো বের করতে চাও

তাহলে `keyof typeof Role` ব্যবহার করবে 👇

```ts
keyof typeof Role
// 👉 Result: "ADMIN" | "USER" | "GUEST"
```

মানে —
“Role object-এর key গুলোর নামের একটা union type তৈরি করো।”

---

## 🔹 কিন্তু আমরা চাই value গুলোর union

তাহলে লিখতে হবে 👇

```ts
type RoleType = typeof Role[keyof typeof Role];
```

এখানে কী হচ্ছে ধাপে ধাপে 👇

1. `typeof Role` → পুরো object-এর টাইপ নিলাম।
2. `keyof typeof Role` → তার key গুলোর নাম পেলাম ("ADMIN" | "USER" | "GUEST")।
3. `typeof Role[keyof typeof Role]` →
   মানে, Role object-এর key গুলোর **ভ্যালু টাইপগুলো** নাও।

✅ তাই `RoleType` এখন:

```ts
type RoleType = "ADMIN" | "USER" | "GUEST";
```

এটা ঠিক enum-এর মতো behave করবে!

---

## 🔹 এখন ব্যবহার করা যাক:

```ts
const person: { name: string; role: RoleType } = {
  name: "Fahim",
  role: Role.ADMIN, // ✅ সঠিক
  // role: "OWNER", ❌ Error: "OWNER" টাই RoleType না
};
```

এখন TypeScript বুঝবে —
`role` শুধুমাত্র `"ADMIN"`, `"USER"`, বা `"GUEST"` হতে পারবে।
অন্য কিছু দিলে সাথে সাথে error দেখাবে।

---

## 🎯 সারসংক্ষেপ

| ধাপ                              | কাজ                                             | ফলাফল                          |
| -------------------------------- | ----------------------------------------------- | ------------------------------ |
| `as const`                       | object টাকে readonly করে, literal টাইপ তৈরি করে | `"ADMIN"` literal              |
| `typeof Role`                    | object-এর টাইপ নেয়                              | `{ ADMIN: "ADMIN"; ... }`      |
| `keyof typeof Role`              | সব key গুলোর নাম নেয়                            | `"ADMIN" \| "USER" \| "GUEST"` |
| `typeof Role[keyof typeof Role]` | সব value-এর টাইপ নেয়                            | `"ADMIN" \| "USER" \| "GUEST"` |

---

তুমি চাইলে আমি একটা ছোট project-style উদাহরণ দেখাতে পারি —
যেখানে `enum` vs `as const` দুইভাবে একই কাজ করা হয়েছে (side-by-side comparison সহ)।
চাও দেখাতে?
*/
